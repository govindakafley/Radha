from django.db import models
import uuid

class RawMaterials(models.Model):
    # purchase_order_id = models.UUIDField(max_length=100,default=uuid.uuid4, editable=False, unique=True)
    # supplier_id = models.UUIDField(max_length=100,default=uuid.uuid4, editable=False, unique=True)
    pass